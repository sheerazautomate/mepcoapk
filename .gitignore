node_modules/

# Secrets — the Convex deploy key lives here. Never commit.
.env
.env.local
.env.*.local

# Capacitor native project (regenerated by `npx cap add android` in CI)
android/
ios/

# Build output
dist/
*.apk
*.aab

.DS_Store
